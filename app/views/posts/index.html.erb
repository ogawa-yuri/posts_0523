<h1>投稿一覧</h1>
<table>
  <tr>
    <th>投稿内容</th>
    <th>投稿日時</th>
  </tr>
<% @posts.each do |post|  %>
  <tr>
    <td><%= post.content %></td>
    <td><%= post.created_at %></td>>
  </tr>
<% end %>
</table>
